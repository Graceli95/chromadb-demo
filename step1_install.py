# Step 1: Installing ChromaDB
# This step shows how to set up ChromaDB in a Python environment

# First, create a virtual environment (if not already done):
# python3 -m venv venv
# source venv/bin/activate  # On Windows: venv\Scripts\activate

# Then install ChromaDB:
# pip install -Uq chromadb
# The -U flag updates to the latest version, -q makes it quiet (less verbose output)

# For Jupyter notebooks, you can also use:
# %pip install -Uq chromadb

print("ChromaDB installation completed successfully!")
