# Python virtual environment
# We don't commit the venv folder because it's large and platform-specific
# Anyone can recreate it using: python -m venv venv && pip install -r requirements.txt
venv/

# Environment variables (contains API keys - never commit!)
.env

# Python cache files
__pycache__/
*.py[cod]
*$py.class

# ChromaDB storage directory (we'll create this in later steps)
chroma_db/

# IDE and editor files
.DS_Store
.vscode/
*.swp
*.swo

# Jupyter Notebook checkpoints
.ipynb_checkpoints/

